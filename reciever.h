#include <vector>

//prototype for function for reciever for error detection and correction

int errorDetection(std::vector<int> ,long long int );

std::vector<int> errorCorrection(std::vector<int> ,long long int );
